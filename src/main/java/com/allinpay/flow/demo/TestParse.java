package com.allinpay.flow.demo;


public class TestParse
{
    public static void main(String[] args)
    {/*
      * 
      * try { File file = new File(
      * "D:\\TLZF_WORK_SPACE\\JBPM_Stuty\\bps-parent\\Bps-test\\src\\main\\java\\com\\allinpay\\flow\\Demo\\2.xml"
      * ); JAXBContext ctx = JAXBContext.newInstance(FlowEngine.class);
      * Unmarshaller unmarshaller = ctx.createUnmarshaller(); FlowEngine Beans =
      * (FlowEngine) unmarshaller.unmarshal(file);
      * 
      * for (Map.Entry<String, Task> entry : Beans.getBeanMap().entrySet()) {
      * System.out.println("key= " + entry.getKey() + " and value= " +
      * entry.getValue().getId());
      * 
      * if ("descisions".equals(entry.getKey())) {
      * System.out.println(entry.getValue().getDescisions().getId());
      * System.out.println(entry.getValue().getDescisions().getBeanName());
      * 
      * Map<String, Descision> map1 =
      * entry.getValue().getDescisions().getBeanMap(); for (Map.Entry<String,
      * Descision> entry1 : map1.entrySet()) { System.out.println("key= " +
      * entry1.getKey() + " and value= " + entry1.getValue().getValue()); } } }
      * } catch (Exception e) { e.printStackTrace(); }
      */
    }
}
